#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"
PYTHONPATH=src python -m pytest
tmp=$(mktemp -d)
trap 'rm -rf "$tmp"' EXIT HUP INT TERM
python -m build --outdir "$tmp"
python -m twine check "$tmp"/*
